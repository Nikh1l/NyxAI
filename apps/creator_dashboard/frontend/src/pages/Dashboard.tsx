import MediaGrid from "../components/MediaGrid";

export default function Dashboard() {
  return (
    <div
      style={{
        padding: 30,
      }}
    >
        <h1>Nyx Creator Dashboard</h1>
        <MediaGrid />
    </div>
  )
}