## ADR 002
Do not assume every model supports every capability.
Capabilities must be benchmarked.

### Reason
Different runtimes expose different APIs.

### Implications
Every capability needs a compatible matrix.